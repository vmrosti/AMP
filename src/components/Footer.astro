---
import { services } from "../data/services.js";
---

<footer class="footer">
  <div class="container footer__grid">
    <div class="footer__brand">
      <img src="/images/logo-lockup-dark.png" alt="AMP-stroje — Advanced mechanical processes" class="footer__logo-img" />
    </div>

    <nav aria-label="Navigace">
      <h2 class="footer__heading">Navigace</h2>
      <ul>
        <li><a href="/sluzby/">Služby</a></li>
        <li><a href="/realizace/">Realizace</a></li>
        <li><a href="/o-nas/">O nás</a></li>
        <li><a href="/kontakt/">Kontakt</a></li>
        <li><a href="/ochrana-osobnich-udaju/">Ochrana osobních údajů</a></li>
      </ul>
    </nav>

    <nav aria-label="Služby">
      <h2 class="footer__heading">Služby</h2>
      <ul>
        {services.map((s) => <li><a href={`/sluzby/${s.slug}/`}>{s.title}</a></li>)}
      </ul>
    </nav>

    <div>
      <h2 class="footer__heading">Kontakt</h2>
      <address>
        AMP-stroje, s.r.o.<br />
        Pražákova 1008/69<br />
        Brno, 639 00<br />
        IČO: 19091842<br />
        <a href="tel:+420777911077" data-event="phone_click">+420 777 911 077</a><br />
        <a href="mailto:info@amp-stroje.cz" data-event="email_click">info@amp-stroje.cz</a>
      </address>
    </div>
  </div>

  <div class="container footer__bottom">
    <p>&copy; {new Date().getFullYear()} AMP-stroje, s.r.o.</p>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-void-soft);
    color: var(--color-steel);
    padding-block: var(--space-7) var(--space-5);
    border-top: 1px solid var(--color-steel-light);
  }

  @media (max-width: 1023px) {
    .footer {
      padding-bottom: calc(var(--space-5) + 4.5rem); /* místo pro sticky mobilní CTA */
    }
  }

  .footer__grid {
    display: grid;
    gap: var(--space-6);
    grid-template-columns: 1fr;
  }

  @media (min-width: 768px) {
    .footer__grid {
      grid-template-columns: 1.5fr 1fr 1fr 1.5fr;
    }
  }

  .footer__logo-img {
    width: 100%;
    max-width: 15rem;
    height: auto;
  }

  .footer__heading {
    font-size: var(--text-small);
    font-family: var(--font-mono);
    color: var(--color-steel);
    margin-bottom: var(--space-3);
  }

  .footer ul {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .footer a {
    color: var(--color-paper-alt);
    text-decoration: none;
    font-size: var(--text-small);
  }

  .footer a:hover {
    color: var(--color-white);
    text-decoration: underline;
  }

  address {
    font-style: normal;
    font-size: var(--text-small);
    line-height: var(--leading-normal);
  }

  .footer__bottom {
    margin-top: var(--space-6);
    padding-top: var(--space-4);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    font-size: var(--text-small);
  }

  .footer__bottom p {
    margin: 0;
  }
</style>
